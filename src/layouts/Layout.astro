---
import Head from '../components/Head.astro';
import SiteHeader from '../components/SiteHeader.vue';
import SiteFooter from '../components/SiteFooter.vue';
import "@fontsource-variable/doto";
import "@fontsource-variable/jetbrains-mono";
import TerminalWindow from '../components/TerminalWindow.vue';
import AnnoyingEyelash from '../components/AnnoyingEyelash.vue';
import {getGitBranch, getLatestCommitHash, getLatestCommitMessage, getGitHubRepoUrl} from '../utils/gitInfo.js';
import EasterEgg from '../components/EasterEgg.vue';
import EasterEggsFound from '../components/EasterEggsFound.vue';

export interface Props {
    title?: string;
    description?: string;
    favicon?: string;
    headerVariant?: string;
    hideFooter?: boolean;
    hideTerminal?: boolean;
    hideHeader?: boolean;
    hideAnnoyingEyelash?: boolean;
    hideEggs?: boolean;
}

const {
    hideHeader = false,
    title,
    description,
    favicon,
    headerVariant = 'default',
    hideFooter = false,
    hideTerminal = false,
    hideAnnoyingEyelash = false,
    hideEggs,
} = Astro.props;
const gitBranch = await getGitBranch();
const commitHash = await getLatestCommitHash();
const commitMessage = await getLatestCommitMessage();
const repoUrl = getGitHubRepoUrl();
---

<html lang="en">
<Head
    title={title}
    description={description}
    favicon={favicon}
>
</Head>
<body>
<TerminalWindow
    client:only="vue"
    title="Nick's Terminal"
    initialOutput={[
        "Welcome to Nick Berens' Terminal",
        "Type 'help' for available commands"
    ]}
    hideTerminal={hideTerminal}
></TerminalWindow>
<SiteHeader
    client:only="vue"
    gitBranch={gitBranch}
    variant={headerVariant}
    hideHeader={hideHeader}
/>
<AnnoyingEyelash
    client:load
    initial-x={50}
    initial-y={75}
    hideAnnoyingEyelash={hideAnnoyingEyelash}
/>
<slot/>
<EasterEgg
    client:only="vue"
    hideEggs={hideEggs}
/>
<EasterEggsFound client:only="vue" />
<SiteFooter
    commitHash={commitHash}
    commitMessage={commitMessage}
    repoUrl={repoUrl}
    hideFooter={hideFooter}
    client:load
/>

<script>
    console.log(`
%c███╗   ██╗██╗ ██████╗██╗  ██╗    █████╗ ██╗
████╗  ██║██║██╔════╝██║ ██╔╝   ██╔══██╗██║
██╔██╗ ██║██║██║     █████╔╝    ███████║██║
██║╚██╗██║██║██║     ██╔═██╗    ██╔══██║██║
██║ ╚████║██║╚██████╗██║  ██╗██╗██║  ██║██║
╚═╝  ╚═══╝╚═╝ ╚═════╝╚═╝  ╚═╝╚═╝╚═╝  ╚═╝╚═╝`,
        "background: linear-gradient(to right, #ff0000, #00ff00, #5959ff);; -webkit-background-clip: text; background-clip: text; -webkit-text-fill-color: transparent; font-weight: bold; font-size: 24px;"
    )
    console.log(
        `%cWelcome to my website!`,
        "color: #00ff00; font-weight: bold; font-size: 20px;"
    );
</script>
</body>
</html>
